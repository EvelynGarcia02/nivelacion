# Insumos internos: CSV fuente (datos crudos), informe PDF, capturas de referencia.
# js/data.js (agregado y anonimizado) SI se versiona -- es lo que usa el dashboard publicado.
data/

# Metadata local de la herramienta (no es parte del proyecto)
.claude/
